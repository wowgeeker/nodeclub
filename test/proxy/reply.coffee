Reply = require("../../proxy/reply")
support = require("../support/support")
should = require("should")
describe "proxy/reply.js", ->
  reply = undefined
  before (done) ->
    support.createUser (err, user) ->
      
      # support.createTopic();
      # Reply.newAndSave(done);
      done()
      return

    return

  describe "getReplyById", ->
    it "should ok", (done) ->
      
      # Reply.getReplyById('');
      done()
      return

    return

  return

